# REQ-172: End-to-End Flow Testing - Detailed Task Breakdown

**Document Created:** 2026-01-09 22:15 UTC
**Last Modified:** 2026-01-09 22:15 UTC
**Request ID:** REQ-172
**Phase:** 6 - Integration & Polish
**Task ID:** 6.1
**Overview Document:** `/docs/REQ-172-end-to-end-flow-testing-overview.md`
**Plan Reference:** `/docs/prd/Plan-094-UI-UX-Workflow-Improvements.md`

---

## Document Purpose

This document breaks down the End-to-End Flow Testing task (REQ-172) into granular, actionable implementation tasks. Each task is designed to be completable within 1 story point (a few hours of focused work) and includes specific verification steps.

---

## Prerequisites

Before starting these tasks, ensure the following are complete:

- [ ] **Phase 1:** Types and Constants (PURPOSE_TYPES, updated state shape)
- [ ] **Phase 2:** PurposeStep component created and integrated
- [ ] **Phase 3:** ContentSourceStep removed, ContentTypeStep consolidated
- [ ] **Phase 4:** Bottom navigation removed from content screens
- [ ] **Phase 5:** PreviewSaveStep redesigned with ContentPreview

---

## Authorized Files for Modification

### Files to CREATE

| File Path | Purpose |
|-----------|---------|
| `src/components/ItemCreationWorkflow/__tests__/e2e/workflow-complete-flow.test.tsx` | Complete flow navigation tests |
| `src/components/ItemCreationWorkflow/__tests__/e2e/workflow-title-generation.test.tsx` | Title generation tests |
| `src/components/ItemCreationWorkflow/__tests__/e2e/workflow-add-more-content.test.tsx` | Add more content flow tests |
| `src/components/ItemCreationWorkflow/__tests__/e2e/workflow-cancel-confirmation.test.tsx` | Cancel confirmation tests |
| `src/components/ItemCreationWorkflow/__tests__/e2e/workflow-state-transitions.test.tsx` | State transition integration tests |
| `src/components/ItemCreationWorkflow/__tests__/e2e/test-utils.ts` | Shared E2E test utilities |
| `src/components/ItemCreationWorkflow/__tests__/e2e/index.ts` | Test suite exports |
| `docs/manual-test-checklist-req-172.md` | Manual testing checklist |

### Files to MODIFY (if needed)

| File Path | Modification |
|-----------|--------------|
| `src/components/ItemCreationWorkflow/__tests__/helpers/testUtils.ts` | Add E2E navigation helpers for new steps |
| `src/components/ItemCreationWorkflow/__tests__/helpers/mockFactories.ts` | Add purpose field to mock factories |
| `src/components/ItemCreationWorkflow/utils/__tests__/titleGenerator.test.ts` | Expand title generation unit tests |

### Files to READ ONLY (for test reference)

| File Path | Reference Purpose |
|-----------|-------------------|
| `src/components/ItemCreationWorkflow/ItemCreationWorkflow.tsx` | Component structure |
| `src/components/ItemCreationWorkflow/hooks/useWorkflowState.ts` | State machine logic |
| `src/components/ItemCreationWorkflow/ItemCreationWorkflow.types.ts` | Type definitions |
| `src/components/ItemCreationWorkflow/utils/constants.ts` | Constants and enums |
| `src/components/ItemCreationWorkflow/utils/titleGenerator.ts` | Title generation logic |
| `src/components/ItemCreationWorkflow/components/steps/PurposeStep.tsx` | Purpose step implementation |
| `src/components/ItemCreationWorkflow/components/steps/PreviewSaveStep.tsx` | Preview step implementation |
| `src/components/ItemCreationWorkflow/components/steps/NextActionStep.tsx` | Next action step implementation |
| `src/components/ItemCreationWorkflow/components/shared/ContentPreview.tsx` | Content preview component |

---

## Task Breakdown

### Task 1: Create E2E Test Directory Structure and Utilities

**Task ID:** 6.1.1
**Effort:** ~2 hours
**Priority:** High
**Dependencies:** None

#### Description

Set up the test directory structure and create shared E2E test utilities that will be used across all E2E test files.

#### Implementation Steps

1. **Create the e2e test directory**
   - Create directory: `src/components/ItemCreationWorkflow/__tests__/e2e/`

2. **Create test-utils.ts with shared utilities**
   - File: `src/components/ItemCreationWorkflow/__tests__/e2e/test-utils.ts`
   - Include:
     - Import statements for testing-library and vitest
     - `NEW_WORKFLOW_STEPS_ORDER` constant reflecting the new flow (without content-source-selection, with purpose-selection)
     - `renderWorkflowForE2E()` helper that wraps render with all required providers
     - `navigateToPurposeStep()` helper function
     - `navigateToContentTypeStep()` helper for new flow
     - `navigateToPreviewSaveStep()` complete navigation helper
     - `createMockFileUpload()` utility for simulating file uploads
     - `waitForContentPreview()` helper to wait for content to appear in preview
     - `assertAutoGeneratedTitle()` helper for title verification

3. **Create index.ts for exports**
   - File: `src/components/ItemCreationWorkflow/__tests__/e2e/index.ts`
   - Export all test utilities and constants

4. **Update existing testUtils.ts**
   - File: `src/components/ItemCreationWorkflow/__tests__/helpers/testUtils.ts`
   - Add `navigateToPurposeSelection()` helper
   - Update `WORKFLOW_STEPS_ORDER` to reflect new step order
   - Add `waitForPurposeStep()` helper
   - Update `STEP_DISPLAY_LABELS` to include purpose-selection label

5. **Update mockFactories.ts**
   - File: `src/components/ItemCreationWorkflow/__tests__/helpers/mockFactories.ts`
   - Add `purpose` field to `createMockCurrentItemState()`
   - Add `createMockCurrentItemStateWithPurpose()` factory
   - Update `getStepHistory()` to include purpose-selection step

#### Verification Steps

- [ ] Run `npm test -- --testPathPattern="e2e/test-utils"` - should find the test utilities
- [ ] Verify TypeScript compilation passes: `npm run type-check`
- [ ] Import test utilities in a test file and verify no type errors
- [ ] Verify new navigation helpers exist in testUtils.ts

#### Acceptance Criteria

- [ ] E2E test directory exists at `src/components/ItemCreationWorkflow/__tests__/e2e/`
- [ ] `test-utils.ts` exports all shared utilities
- [ ] `index.ts` re-exports all utilities
- [ ] Updated helpers support the new workflow step order
- [ ] Mock factories include purpose field

---

### Task 2: Implement Complete Flow Navigation Tests - Happy Path

**Task ID:** 6.1.2a
**Effort:** ~2 hours
**Priority:** High
**Dependencies:** Task 6.1.1

#### Description

Create tests for the complete happy path flow from room selection through save, verifying each step transition works correctly.

#### Implementation Steps

1. **Create workflow-complete-flow.test.tsx**
   - File: `src/components/ItemCreationWorkflow/__tests__/e2e/workflow-complete-flow.test.tsx`

2. **Implement happy path test suite**
   ```typescript
   describe('Complete Flow Navigation - Happy Path', () => {
     it('completes workflow: Room -> Item Type -> Specific Item -> Purpose -> Content Type -> Create -> Review -> Save', async () => {
       // Test implementation
     });
   });
   ```

3. **Test implementation details**
   - Render workflow with mock props
   - Select room (e.g., "Kitchen")
   - Verify transition to item-type-selection
   - Select item type (e.g., "Appliance")
   - Verify transition to specific-item-selection
   - Enter/select specific item (e.g., "Fridge")
   - Verify transition to purpose-selection (NEW)
   - Select purpose (e.g., "how-to-clean")
   - Verify transition to content-type-selection
   - Select content type (e.g., "Upload File")
   - Verify transition to content-creation
   - Simulate file upload
   - Verify transition to preview-save
   - Verify all fields displayed correctly
   - Click Save
   - Verify `onSaveItem` called with correct data structure

4. **Add parameterized tests for different paths**
   ```typescript
   describe.each([
     ['kitchen', 'appliance', 'Fridge', 'how-to-clean'],
     ['laundry', 'appliance', 'Washer', 'how-to-use'],
     ['bedroom', 'room-item', 'Bed', 'maintenance'],
   ])('flow with room=%s, type=%s, item=%s, purpose=%s', ...)
   ```

#### Verification Steps

- [ ] Run: `npm test -- --testPathPattern="workflow-complete-flow"`
- [ ] All happy path tests pass
- [ ] Test covers all new workflow steps
- [ ] Verify mocks are called with expected arguments

#### Acceptance Criteria

- [ ] Happy path test completes full workflow
- [ ] All step transitions are verified
- [ ] Purpose step is included in flow
- [ ] Save callback receives correct data structure
- [ ] Parameterized tests cover multiple room/item/purpose combinations

---

### Task 3: Implement Complete Flow Navigation Tests - Back Navigation

**Task ID:** 6.1.2b
**Effort:** ~2 hours
**Priority:** High
**Dependencies:** Task 6.1.2a

#### Description

Add tests for back navigation functionality at each step in the workflow.

#### Implementation Steps

1. **Add back navigation test suite to workflow-complete-flow.test.tsx**
   ```typescript
   describe('Back Navigation', () => {
     it('navigates back from item-type-selection to room-selection', async () => {});
     it('navigates back from specific-item-selection to item-type-selection', async () => {});
     it('navigates back from purpose-selection to specific-item-selection', async () => {});
     it('navigates back from content-type-selection to purpose-selection', async () => {});
     it('navigates back from content-creation to content-type-selection', async () => {});
     it('navigates back from preview-save to content-creation', async () => {});
   });
   ```

2. **Test implementation pattern for each**
   - Navigate to target step
   - Click Back button
   - Verify returned to previous step
   - Verify state preserved (selections not lost)

3. **Add test for multi-step back navigation**
   ```typescript
   it('preserves state through multiple back-forward navigations', async () => {
     // Navigate to purpose-selection
     // Go back to specific-item
     // Go forward again
     // Verify purpose selection preserved
   });
   ```

4. **Add test for step history**
   ```typescript
   it('maintains correct step history through navigation', async () => {
     // Navigate forward several steps
     // Navigate back
     // Verify stepHistory state is correct
   });
   ```

#### Verification Steps

- [ ] Run: `npm test -- --testPathPattern="workflow-complete-flow" --testNamePattern="Back Navigation"`
- [ ] All back navigation tests pass
- [ ] State preservation is verified at each step

#### Acceptance Criteria

- [ ] Back navigation works at every step
- [ ] State is preserved when navigating back
- [ ] Step history is maintained correctly
- [ ] Multi-step navigation works as expected

---

### Task 4: Implement Complete Flow Navigation Tests - Edge Cases

**Task ID:** 6.1.2c
**Effort:** ~2 hours
**Priority:** Medium
**Dependencies:** Task 6.1.2b

#### Description

Add tests for edge cases and special navigation scenarios.

#### Implementation Steps

1. **Add edge case tests to workflow-complete-flow.test.tsx**
   ```typescript
   describe('Edge Cases', () => {
     it('handles "general" room type by skipping item-type-selection', async () => {});
     it('handles "other" purpose type correctly', async () => {});
     it('handles empty specific item name gracefully', async () => {});
     it('prevents navigation forward when required fields missing', async () => {});
   });
   ```

2. **Test for session recovery (if applicable)**
   ```typescript
   it('recovers workflow state from localStorage on remount', async () => {
     // Set up localStorage with partial session
     // Render workflow
     // Verify state recovered
   });
   ```

3. **Test for error states**
   ```typescript
   describe('Error Handling', () => {
     it('displays error when save fails', async () => {});
     it('allows retry after save failure', async () => {});
     it('handles network error during file upload', async () => {});
   });
   ```

#### Verification Steps

- [ ] Run: `npm test -- --testPathPattern="workflow-complete-flow" --testNamePattern="Edge Cases"`
- [ ] All edge case tests pass
- [ ] Error handling tests verify appropriate UI feedback

#### Acceptance Criteria

- [ ] General room type flow tested
- [ ] Other purpose type handled
- [ ] Required field validation tested
- [ ] Error states display correct feedback
- [ ] Session recovery works (if applicable)

---

### Task 5: Implement Title Generation Tests - Unit Tests

**Task ID:** 6.1.3a
**Effort:** ~1.5 hours
**Priority:** High
**Dependencies:** Task 6.1.1

#### Description

Create comprehensive unit tests for the `titleGenerator` utility to verify all purpose/item combinations generate correct titles.

#### Implementation Steps

1. **Create or expand titleGenerator.test.ts**
   - File: `src/components/ItemCreationWorkflow/utils/__tests__/titleGenerator.test.ts`

2. **Implement parameterized tests for all purpose types**
   ```typescript
   describe('generateArticleTitle', () => {
     it.each([
       ['how-to-use', 'Oven', 'How to Use - Oven'],
       ['how-to-clean', 'Fridge', 'How to Clean - Fridge'],
       ['troubleshooting', 'Dishwasher', 'Troubleshooting - Dishwasher'],
       ['safety-info', 'Stove', 'Safety Information - Stove'],
       ['maintenance', 'HVAC', 'Maintenance - HVAC'],
       ['features', 'Smart TV', 'Features & Tips - Smart TV'],
       ['other', 'Custom Item', 'Custom Item'],
     ])('generates "%s" → "%s" for item "%s"', (purpose, item, expected) => {
       expect(generateArticleTitle({ specificItem: item, purpose })).toBe(expected);
     });
   });
   ```

3. **Test fallback behavior**
   ```typescript
   describe('Fallback Behavior', () => {
     it('returns item name when purpose is null', () => {
       expect(generateArticleTitle({ specificItem: 'Fridge', purpose: null })).toBe('Fridge');
     });

     it('returns item name when purpose is undefined', () => {
       expect(generateArticleTitle({ specificItem: 'Fridge', purpose: undefined })).toBe('Fridge');
     });

     it('handles empty item name', () => {
       expect(generateArticleTitle({ specificItem: '', purpose: 'how-to-use' })).toBe('How to Use - ');
     });
   });
   ```

4. **Test special characters in item names**
   ```typescript
   describe('Special Characters', () => {
     it('handles item names with special characters', () => {
       expect(generateArticleTitle({ specificItem: 'LG Smart TV (65")', purpose: 'how-to-use' }))
         .toBe('How to Use - LG Smart TV (65")');
     });

     it('handles item names with hyphens', () => {
       expect(generateArticleTitle({ specificItem: 'All-in-One Washer-Dryer', purpose: 'maintenance' }))
         .toBe('Maintenance - All-in-One Washer-Dryer');
     });
   });
   ```

#### Verification Steps

- [ ] Run: `npm test -- --testPathPattern="titleGenerator"`
- [ ] All unit tests pass
- [ ] Coverage report shows 100% coverage for titleGenerator

#### Acceptance Criteria

- [ ] All 7 purpose types have test coverage
- [ ] Null/undefined purpose fallback tested
- [ ] Edge cases with special characters tested
- [ ] Empty string handling tested

---

### Task 6: Implement Title Generation Tests - Integration Tests

**Task ID:** 6.1.3b
**Effort:** ~1.5 hours
**Priority:** High
**Dependencies:** Task 6.1.3a, Task 6.1.2a

#### Description

Create integration tests verifying the auto-generated title appears correctly in the UI at the PreviewSaveStep.

#### Implementation Steps

1. **Create workflow-title-generation.test.tsx**
   - File: `src/components/ItemCreationWorkflow/__tests__/e2e/workflow-title-generation.test.tsx`

2. **Test title display in PreviewSaveStep**
   ```typescript
   describe('Auto-Generated Title in UI', () => {
     it('displays auto-generated title in PreviewSaveStep', async () => {
       // Navigate to preview-save with purpose "how-to-clean" and item "Fridge"
       // Verify title field shows "How to Clean - Fridge"
     });

     it('updates title when purpose changes', async () => {
       // This tests the case where user goes back and changes purpose
     });
   });
   ```

3. **Test title editing functionality**
   ```typescript
   describe('Title Editing', () => {
     it('allows editing the auto-generated title', async () => {
       // Navigate to preview-save
       // Click edit on title field
       // Change title to custom value
       // Verify new title persists
     });

     it('preserves custom title when adding more content', async () => {
       // Edit title
       // Add more content
       // Return to preview
       // Verify custom title still shown
     });
   });
   ```

4. **Test title in saved data**
   ```typescript
   it('includes correct title in saved item data', async () => {
     // Complete flow
     // Verify onSaveItem called with correct title in payload
   });
   ```

#### Verification Steps

- [ ] Run: `npm test -- --testPathPattern="workflow-title-generation"`
- [ ] All integration tests pass
- [ ] Title appears correctly in UI
- [ ] Title editing works

#### Acceptance Criteria

- [ ] Auto-generated title displays in preview step
- [ ] Title is editable
- [ ] Custom title persists through navigation
- [ ] Saved data includes correct title

---

### Task 7: Implement Pre-filled Fields Tests

**Task ID:** 6.1.4
**Effort:** ~2 hours
**Priority:** High
**Dependencies:** Task 6.1.2a

#### Description

Create tests verifying all pre-filled fields display correctly in the PreviewSaveStep.

#### Implementation Steps

1. **Add tests to workflow-complete-flow.test.tsx or create new file**
   - Consider adding to existing complete flow tests or separate file

2. **Test room field display**
   ```typescript
   describe('Pre-filled Fields in PreviewSaveStep', () => {
     it('displays selected room correctly', async () => {
       // Select "Kitchen" in room-selection
       // Navigate to preview-save
       // Verify "Kitchen" displayed in Room field
       // Verify field is read-only
     });
   });
   ```

3. **Test item type field display**
   ```typescript
   it('displays selected item type correctly', async () => {
     // Select "Appliance" in item-type-selection
     // Navigate to preview-save
     // Verify "Appliance" displayed in Item Type field
     // Verify field is read-only
   });
   ```

4. **Test purpose field display**
   ```typescript
   it('displays selected purpose correctly', async () => {
     // Select "how-to-clean" in purpose-selection
     // Navigate to preview-save
     // Verify "How to Clean" displayed in Purpose field
     // Verify field is read-only
   });
   ```

5. **Test specific item display**
   ```typescript
   it('displays specific item name correctly', async () => {
     // Enter "Refrigerator" in specific-item-selection
     // Navigate to preview-save
     // Verify "Refrigerator" displayed
   });
   ```

6. **Test content preview display**
   ```typescript
   describe('Content Preview Display', () => {
     it('displays video thumbnail for uploaded video', async () => {
       // Upload video file
       // Navigate to preview-save
       // Verify video thumbnail displayed (not placeholder)
     });

     it('displays image preview for uploaded photo', async () => {});
     it('displays PDF preview with page count', async () => {});
     it('displays truncated text preview', async () => {});
     it('displays URL preview with domain', async () => {});
   });
   ```

#### Verification Steps

- [ ] Run: `npm test -- --testPathPattern="Pre-filled Fields"`
- [ ] All field display tests pass
- [ ] Content previews render correctly

#### Acceptance Criteria

- [ ] Room displays correctly (read-only)
- [ ] Item Type displays correctly (read-only)
- [ ] Purpose displays correctly (read-only)
- [ ] Title displays correctly (editable)
- [ ] Content previews show actual content (not placeholders)
- [ ] Each content type (video, photo, PDF, text, URL) has preview test

---

### Task 8: Implement "Add More Content" Flow Tests

**Task ID:** 6.1.5
**Effort:** ~2.5 hours
**Priority:** Medium
**Dependencies:** Task 6.1.4

#### Description

Create comprehensive tests for the "Add More Content" flow functionality.

#### Implementation Steps

1. **Create workflow-add-more-content.test.tsx**
   - File: `src/components/ItemCreationWorkflow/__tests__/e2e/workflow-add-more-content.test.tsx`

2. **Test basic add more flow**
   ```typescript
   describe('Add More Content Flow', () => {
     it('allows adding multiple content pieces to same item', async () => {
       // Create first content piece (video upload)
       // Navigate to preview-save
       // Click "Add More Content"
       // Verify returns to content-type-selection
       // Create second content piece (photo)
       // Navigate to preview-save
       // Verify both pieces displayed
     });
   });
   ```

3. **Test content count badge**
   ```typescript
   it('updates content count badge when adding more', async () => {
     // Add first piece → verify count badge shows "1"
     // Add more content
     // Add second piece → verify count badge shows "2"
     // Add more → verify count badge shows "3"
   });
   ```

4. **Test content order preservation**
   ```typescript
   it('preserves content order after adding more', async () => {
     // Add pieces in order: video, photo, text
     // Verify order preserved in preview display
     // Content order should match addition order
   });
   ```

5. **Test content limit (if applicable)**
   ```typescript
   it('enforces maximum content limit when reached', async () => {
     // Add content up to configured max limit
     // Verify "Add More" option disabled or hidden
     // Or verify user receives feedback about limit
   });
   ```

6. **Test state preservation during add more**
   ```typescript
   it('maintains item state when navigating back from add more', async () => {
     // Add content
     // Add more (goes to content-type-selection)
     // Navigate back
     // Verify previous content still present
     // Verify all item selections preserved
   });
   ```

7. **Test multiple content type combinations**
   ```typescript
   describe.each([
     [['video', 'photo']],
     [['video', 'text', 'url']],
     [['pdf', 'video']],
   ])('with content types %s', (contentTypes) => {
     it('handles multiple content types correctly', async () => {});
   });
   ```

#### Verification Steps

- [ ] Run: `npm test -- --testPathPattern="workflow-add-more-content"`
- [ ] All add more content tests pass
- [ ] Multiple content pieces display correctly

#### Acceptance Criteria

- [ ] Add More navigates to content-type-selection
- [ ] Multiple content pieces associate with same item
- [ ] Content count badge updates correctly
- [ ] Content order is preserved
- [ ] State preserved through add more navigation
- [ ] Maximum limit enforced (if applicable)

---

### Task 9: Implement Cancel Confirmation Dialog Tests

**Task ID:** 6.1.6
**Effort:** ~2 hours
**Priority:** High
**Dependencies:** Task 6.1.2a

#### Description

Create comprehensive tests for the Cancel confirmation dialog behavior.

#### Implementation Steps

1. **Create workflow-cancel-confirmation.test.tsx**
   - File: `src/components/ItemCreationWorkflow/__tests__/e2e/workflow-cancel-confirmation.test.tsx`

2. **Test no dialog when no content**
   ```typescript
   describe('Cancel with No Content', () => {
     it('exits directly when no content has been added', async () => {
       // Navigate to content-type-selection (no content yet)
       // Click Cancel
       // Verify no confirmation dialog shown
       // Verify onSessionExit called immediately
     });
   });
   ```

3. **Test dialog appears after content upload**
   ```typescript
   describe('Cancel with Content', () => {
     it('shows confirmation when file has been uploaded', async () => {
       // Upload a file
       // Click Cancel
       // Verify confirmation dialog appears
       // Verify dialog message warns about losing work
     });

     it('shows confirmation when text has been entered', async () => {
       // Enter text content
       // Click Cancel
       // Verify confirmation dialog appears
     });

     it('shows confirmation when video has been recorded', async () => {
       // Simulate video recording
       // Click Cancel
       // Verify confirmation dialog appears
     });
   });
   ```

4. **Test confirm cancel behavior**
   ```typescript
   describe('Confirm Cancel', () => {
     it('discards content when cancel is confirmed', async () => {
       // Add content
       // Click Cancel
       // Click "Yes, Cancel" in dialog
       // Verify onSessionExit called
       // Verify content state cleared
     });
   });
   ```

5. **Test dismiss cancel behavior**
   ```typescript
   describe('Dismiss Cancel', () => {
     it('preserves content when cancel is dismissed', async () => {
       // Add content
       // Click Cancel
       // Click "Keep Working" in dialog
       // Verify returned to workflow
       // Verify content still present
     });
   });
   ```

6. **Test cancel from multiple steps**
   ```typescript
   describe('Cancel from Different Steps', () => {
     it('shows confirmation from content-creation step', async () => {});
     it('shows confirmation from preview-save step', async () => {});
     it('shows confirmation from next-action step', async () => {});
   });
   ```

7. **Test dialog accessibility**
   ```typescript
   describe('Dialog Accessibility', () => {
     it('dialog has correct aria attributes', async () => {
       // Verify role="alertdialog"
       // Verify aria-labelledby
       // Verify aria-describedby
     });

     it('traps focus within dialog', async () => {
       // Open dialog
       // Tab through elements
       // Verify focus stays within dialog
     });

     it('closes on Escape key', async () => {
       // Open dialog
       // Press Escape
       // Verify dialog closes (dismiss behavior)
     });
   });
   ```

#### Verification Steps

- [ ] Run: `npm test -- --testPathPattern="workflow-cancel-confirmation"`
- [ ] All cancel confirmation tests pass
- [ ] Dialog accessibility tests pass

#### Acceptance Criteria

- [ ] No dialog when no content added
- [ ] Dialog appears after any content creation (upload, text, recording)
- [ ] Confirm discards content and exits
- [ ] Dismiss preserves content and returns to workflow
- [ ] Dialog shows from multiple workflow stages
- [ ] Dialog meets accessibility requirements

---

### Task 10: Implement State Transition Integration Tests

**Task ID:** 6.1.7
**Effort:** ~2 hours
**Priority:** Medium
**Dependencies:** Task 6.1.2c

#### Description

Create integration tests that verify the state machine transitions correctly through the new workflow steps.

#### Implementation Steps

1. **Create workflow-state-transitions.test.tsx**
   - File: `src/components/ItemCreationWorkflow/__tests__/e2e/workflow-state-transitions.test.tsx`

2. **Test state shape at each step**
   ```typescript
   describe('State Transitions', () => {
     it('transitions correctly through new workflow steps', async () => {
       // Custom render that exposes state
       // Verify initial state.currentStep === 'room-selection'

       // After room selection
       // Verify state.currentStep === 'item-type-selection'
       // Verify state.currentItem.room === 'kitchen'

       // After item type selection
       // Verify state.currentStep === 'specific-item-selection'
       // Verify state.currentItem.itemType === 'appliance'

       // After specific item selection
       // Verify state.currentStep === 'purpose-selection'
       // Verify state.currentItem.specificItem === 'Fridge'

       // After purpose selection
       // Verify state.currentStep === 'content-type-selection'
       // Verify state.currentItem.purpose === 'how-to-clean'

       // Continue through remaining steps...
     });
   });
   ```

3. **Test currentItem state updates**
   ```typescript
   it('updates currentItem state correctly at each step', async () => {
     // Verify currentItem.room after room selection
     // Verify currentItem.itemType after item type selection
     // Verify currentItem.specificItem after specific item
     // Verify currentItem.purpose after purpose selection
     // Verify currentItem.content array after content creation
   });
   ```

4. **Test stepHistory tracking**
   ```typescript
   it('maintains correct stepHistory through navigation', async () => {
     // Navigate through several steps
     // Verify stepHistory contains correct sequence
     // Navigate back
     // Verify stepHistory updated correctly
   });
   ```

5. **Test isDirty flag**
   ```typescript
   describe('isDirty Flag', () => {
     it('sets isDirty to true when content is added', async () => {
       // Add content
       // Verify state.isDirty === true
     });

     it('keeps isDirty false before content creation', async () => {
       // Navigate to content-type-selection
       // Verify state.isDirty === false
     });
   });
   ```

6. **Test session state**
   ```typescript
   describe('Session State', () => {
     it('updates session.items after save', async () => {
       // Complete and save item
       // Verify item added to session.items array
     });

     it('maintains session across multiple items', async () => {
       // Save first item
       // Add another item
       // Save second item
       // Verify session.items contains both
     });
   });
   ```

#### Verification Steps

- [ ] Run: `npm test -- --testPathPattern="workflow-state-transitions"`
- [ ] All state transition tests pass
- [ ] State shape matches expected structure at each step

#### Acceptance Criteria

- [ ] State transitions correctly through all steps
- [ ] currentItem updated correctly at each step
- [ ] stepHistory maintains correct sequence
- [ ] isDirty flag behaves correctly
- [ ] Session state persists across items

---

### Task 11: Create Manual Test Checklist Document

**Task ID:** 6.1.8
**Effort:** ~1 hour
**Priority:** Medium
**Dependencies:** None (can run in parallel)

#### Description

Create a comprehensive manual testing checklist for scenarios difficult to automate.

#### Implementation Steps

1. **Create manual test checklist document**
   - File: `docs/manual-test-checklist-req-172.md`

2. **Document header**
   ```markdown
   # REQ-172 Manual Test Checklist

   **Document Created:** [DATE]
   **Last Modified:** [DATE]
   **Tester:** ________________
   **Test Date:** ________________
   **Environment:** ________________
   ```

3. **Visual inspection tests**
   ```markdown
   ## Visual Inspection Tests

   ### Content Previews
   - [ ] Video thumbnails display correctly (not broken images)
   - [ ] Video duration badge shows correct time format
   - [ ] Photo previews maintain aspect ratio
   - [ ] PDF previews show document icon with page count
   - [ ] Text content shows truncated preview with ellipsis
   - [ ] URL previews show favicon and domain

   ### Layout and Spacing
   - [ ] All cards have consistent spacing
   - [ ] Buttons are properly aligned
   - [ ] No text overflow or clipping
   - [ ] Progress indicator shows correct step count
   ```

4. **Animation and transition tests**
   ```markdown
   ## Animation and Transitions

   - [ ] Step transitions are smooth (no flicker)
   - [ ] Loading states display correctly
   - [ ] Dialog open/close animations work
   - [ ] Content preview fade-in works
   ```

5. **Mobile-specific tests**
   ```markdown
   ## Mobile Device Tests

   ### Touch Interactions
   - [ ] All buttons respond to tap
   - [ ] Drag-to-reorder content works on touch screens
   - [ ] Swipe gestures don't interfere with scroll
   - [ ] No double-tap zoom issues

   ### Viewport Sizes
   - [ ] Layout correct at 320px width
   - [ ] Layout correct at 375px width (iPhone SE)
   - [ ] Layout correct at 414px width (iPhone Plus)
   - [ ] Touch targets minimum 48px
   ```

6. **Accessibility tests**
   ```markdown
   ## Accessibility Tests

   ### Screen Reader
   - [ ] Step announcements read correctly
   - [ ] Form labels announced
   - [ ] Error messages announced
   - [ ] Dialog content announced when opened

   ### Keyboard Navigation
   - [ ] Tab order is logical
   - [ ] Focus visible on all interactive elements
   - [ ] Enter/Space activates buttons
   - [ ] Escape closes dialogs
   - [ ] Arrow keys work in selection grids

   ### Focus Management
   - [ ] Focus moves to new step on transition
   - [ ] Focus trapped in dialogs
   - [ ] Focus returns to trigger after dialog close
   ```

7. **Cross-browser tests**
   ```markdown
   ## Cross-Browser Tests

   Test on each browser:
   - [ ] Chrome (latest)
   - [ ] Firefox (latest)
   - [ ] Safari (latest)
   - [ ] Edge (latest)
   - [ ] Mobile Safari (iOS)
   - [ ] Chrome Mobile (Android)
   ```

#### Verification Steps

- [ ] Document created at correct path
- [ ] All test categories included
- [ ] Checklist items are actionable and specific

#### Acceptance Criteria

- [ ] Manual test checklist document exists
- [ ] Visual inspection tests documented
- [ ] Mobile-specific tests documented
- [ ] Accessibility tests documented
- [ ] Cross-browser tests documented
- [ ] Document includes tester sign-off section

---

### Task 12: Run Full Test Suite and Fix Issues

**Task ID:** 6.1.9
**Effort:** ~2 hours
**Priority:** High
**Dependencies:** Tasks 6.1.1 - 6.1.8

#### Description

Execute the complete E2E test suite and address any failing tests or issues discovered.

#### Implementation Steps

1. **Run full E2E test suite**
   ```bash
   npm test -- --testPathPattern="e2e" --coverage
   ```

2. **Document test results**
   - Record total tests run
   - Record passing tests
   - Record failing tests
   - Record coverage percentage

3. **Fix any failing tests**
   - Identify root cause of failures
   - Update test code or implementation as needed
   - Re-run tests to verify fixes

4. **Check test coverage**
   - Review coverage report for E2E tests
   - Identify any uncovered scenarios
   - Add additional tests if needed

5. **Update test utilities if needed**
   - Fix any helper function issues discovered
   - Improve mock factories based on test results
   - Document any workarounds needed

6. **Final verification**
   ```bash
   # Run all workflow tests (unit + integration + E2E)
   npm test -- --testPathPattern="ItemCreationWorkflow" --coverage
   ```

#### Verification Steps

- [ ] All E2E tests pass: `npm test -- --testPathPattern="e2e"`
- [ ] No TypeScript errors: `npm run type-check`
- [ ] Coverage meets threshold (if defined)
- [ ] No console errors during test runs

#### Acceptance Criteria

- [ ] All E2E tests pass
- [ ] No TypeScript errors in test files
- [ ] Test coverage documented
- [ ] Any test issues fixed and documented

---

## Summary

| Task ID | Task Name | Effort | Priority | Dependencies |
|---------|-----------|--------|----------|--------------|
| 6.1.1 | Create E2E Test Directory Structure and Utilities | ~2 hrs | High | None |
| 6.1.2a | Complete Flow Tests - Happy Path | ~2 hrs | High | 6.1.1 |
| 6.1.2b | Complete Flow Tests - Back Navigation | ~2 hrs | High | 6.1.2a |
| 6.1.2c | Complete Flow Tests - Edge Cases | ~2 hrs | Medium | 6.1.2b |
| 6.1.3a | Title Generation Tests - Unit Tests | ~1.5 hrs | High | 6.1.1 |
| 6.1.3b | Title Generation Tests - Integration Tests | ~1.5 hrs | High | 6.1.3a |
| 6.1.4 | Pre-filled Fields Tests | ~2 hrs | High | 6.1.2a |
| 6.1.5 | "Add More Content" Flow Tests | ~2.5 hrs | Medium | 6.1.4 |
| 6.1.6 | Cancel Confirmation Dialog Tests | ~2 hrs | High | 6.1.2a |
| 6.1.7 | State Transition Integration Tests | ~2 hrs | Medium | 6.1.2c |
| 6.1.8 | Create Manual Test Checklist | ~1 hr | Medium | None |
| 6.1.9 | Run Full Test Suite and Fix Issues | ~2 hrs | High | 6.1.1-6.1.8 |

**Total Estimated Effort:** ~21 hours

---

## Execution Order

Recommended execution sequence:

1. **Parallel Group 1** (can run simultaneously):
   - Task 6.1.1: Create E2E Test Directory Structure
   - Task 6.1.8: Create Manual Test Checklist

2. **Sequential Group** (depends on 6.1.1):
   - Task 6.1.2a: Happy Path Tests
   - Task 6.1.2b: Back Navigation Tests
   - Task 6.1.2c: Edge Case Tests

3. **Parallel Group 2** (depends on 6.1.2a):
   - Task 6.1.3a + 6.1.3b: Title Generation Tests
   - Task 6.1.4: Pre-filled Fields Tests
   - Task 6.1.6: Cancel Confirmation Tests

4. **Sequential Group 2**:
   - Task 6.1.5: Add More Content Tests (depends on 6.1.4)
   - Task 6.1.7: State Transition Tests (depends on 6.1.2c)

5. **Final**:
   - Task 6.1.9: Run Full Suite and Fix Issues

---

## Risk Mitigation

| Risk | Mitigation Strategy |
|------|---------------------|
| New components not yet implemented | Coordinate with Phase 1-5 completion; use mocks if components delayed |
| File upload mocking complexity | Use robust mock utilities from existing test helpers |
| Flaky async tests | Use `waitFor` with appropriate timeouts; avoid arbitrary delays |
| State machine changes during testing | Keep tests modular; update test-utils centrally |

---

## References

- **Request:** `/docs/gen_requests.md` (REQ-172)
- **Overview Document:** `/docs/REQ-172-end-to-end-flow-testing-overview.md`
- **Implementation Plan:** `/docs/prd/Plan-094-UI-UX-Workflow-Improvements.md`
- **Existing Test Helpers:** `/src/components/ItemCreationWorkflow/__tests__/helpers/`
- **Workflow Types:** `/src/components/ItemCreationWorkflow/ItemCreationWorkflow.types.ts`
- **State Machine:** `/src/components/ItemCreationWorkflow/hooks/useWorkflowState.ts`
