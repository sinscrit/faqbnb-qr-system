# Repository Agent Instructions

## Browser Testing

- For local UI inspection and browser automation, use the standalone Playwright MCP browser workflow (`browser-init` / `mcp__playwright__browser_*`).
- Do not use the in-app browser for this repository.
- Start the local application separately, then navigate the Playwright MCP browser to its localhost URL.
